# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
# Comma-separated list of Telegram chat IDs (e.g., "12345,67890")
TELEGRAM_ADMIN_IDS=your_chat_id_here

# API Configuration - Hybrid Architecture
# ==================================================
# The bot uses two separate APIs for optimal performance:
# 1. Search API (Indexer): Provides detailed project metadata
# 2. Counters API (Watcher): Provides real-time unit availability counts

# Search API: Fetches complete project details (name, price, location, developer, etc.)
# Used by: Indexer (runs every INDEXER_INTERVAL minutes)
SEARCH_API_URL=https://sakani.sa/marketplaceApi/search/v3/location?filter%5Bmarketplace_purpose%5D=buy&filter%5Bnhc%5D=false&filter%5Bproduct_types%5D=lands&filter%5Btarget_segment_info%5D=beneficiary&filter%5Bland_type%5D=moh_lands&filter%5Bmode%5D=maps&filter%5Bpurchasing_power%5D=0&filter%5Buse_default_listing%5D=false&sort=-views_count

# Counters API: Fetches live unit counts for fast monitoring
# Used by: Watcher (runs every WATCHER_INTERVAL seconds)
# Response format: {"buy_units_count": {"1004": 11, "1002": 68}}
COUNTERS_API_URL=https://sakani.sa/marketplaceApi/available-units-count

# Polling Intervals
# ==================================================

# INDEXER_INTERVAL: How often to fetch full project details (in minutes)
# Recommended: 60 minutes (project metadata changes infrequently)
# This updates project names, prices, locations, and other details
INDEXER_INTERVAL=60

# WATCHER_INTERVAL: How often to check unit availability (in seconds)
# Recommended: 10 seconds for real-time alerts
# Start with 30 seconds initially, then decrease after monitoring API stability
# This is the critical interval for catching new availability
WATCHER_INTERVAL=10

# Database Configuration
DB_PATH=./data/projects.db
