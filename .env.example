# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
# Comma-separated list of Telegram chat IDs (e.g., "12345,67890")
TELEGRAM_ADMIN_IDS=your_chat_id_here

# API Configuration - Verify-Before-Alert Strategy
# ==================================================
# The bot uses a two-step validation process:
# 1. Counters API: Fast polling for unit count changes (every 10s)
# 2. Validation API: Per-project verification before sending alerts

# Counters API: Monitors unit availability changes across all projects
# Response format: {"buy_units_count": {"1004": 11, "1002": 68}}
COUNTERS_API_URL=https://sakani.sa/marketplaceApi/available-units-count

# WATCHER_INTERVAL: How often to poll the Counters API (in seconds)
# Recommended: 10 seconds for real-time alerts
# When a 0→N transition is detected, the bot validates with mainIntermediaryApi before alerting
WATCHER_INTERVAL=10

# Database Configuration
DB_PATH=./data/projects.db

# Optional: Search API (not used in Verify-Before-Alert mode, kept for compatibility)
SEARCH_API_URL=https://sakani.sa/marketplaceApi/search/v3/location?filter%5Bmarketplace_purpose%5D=buy&filter%5Bnhc%5D=false&filter%5Bproduct_types%5D=lands&filter%5Btarget_segment_info%5D=beneficiary&filter%5Bland_type%5D=moh_lands&filter%5Bmode%5D=maps&filter%5Bpurchasing_power%5D=0&filter%5Buse_default_listing%5D=false&sort=-views_count
